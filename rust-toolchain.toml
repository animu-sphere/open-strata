# Pin the toolchain so every build (local, CI, release) uses the same compiler.
# Bump deliberately; keep `rust-version` in Cargo.toml (the MSRV floor) in sync.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
