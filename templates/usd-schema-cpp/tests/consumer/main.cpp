#include <{{name}}/contractAPI.h>
#include <{{name}}/tokens.h>

int main()
{
    // Link/compile smoke check against the installed target only.
    pxr::{{Name}}ContractAPI schema;
    (void)schema;
    return 0;
}
