# v0.23.10 imaging on the usd profile

Date: 2026-09-26. Intake: [VRM report 50](https://github.com/animu-sphere/usd-vrm-plugins/blob/main/docs/reports/ost/50-2026-09-26-v0.23.9-the-imaging-kind-arrives-and-the-usd-profile-cannot-select-it.md).

## Native evidence

A fresh two-member workspace was generated with the candidate CLI: codeless
`material-schema` and `material-adapter` from imaging template 0.1.1. Its project
profile is `usd`. `ost lock` pinned the existing published Windows OpenUSD 26.08
runtime, source `artifact`, digest
`sha256:3a4e399351763a5ef55575e5341b28adabf1ffe6cf55a1d3cc315e19aba9dbff`.
Its capabilities do not include `hydra-preview`. No runtime was republished or
adopted, and the lock stayed current throughout verification.

The schema generator registers the schema under alias `API`; the verification
provider's `provides` and smoke fixture were aligned to that registered name.
The imaging consumer declares `usd-imaging:API` and its schema bundle edge.

| Check | Result |
| --- | --- |
| Schema and imaging builds with the project-default usd profile | PASS |
| `plugin test --workspace --up-to 4` | PASS, both members |
| Imaging L1 SDK check, L2 construction, L3 read and L4 stage open | PASS, 13 checks passed, 0 failed |
| `plugin package --workspace --product` | PASS, two exact member packages |
| `plugin test --workspace --from-package --up-to 4` | PASS, both extracted packages |
| `lock --check` after packaging and verification | PASS |
| `USDIMAGING_ENABLE_PLUGINS=0` | Expected L2 failure naming the variable |

## Regression coverage

The CLI regression runs a generated imaging descriptor through `usd` selection
for standalone build/test and workspace tests. Its core/mock SDK reaches the
SDK-specific rejection instead of failing the profile capability gate. A
descriptor explicitly requesting `hydra-preview` still fails profile selection.
Unit coverage retains the real/mock/missing SDK diagnostic matrix.

All 935 local workspace tests passed, as did clippy with warnings denied,
formatting and reference-document generation. Attribution was regenerated;
only the twelve workspace crate versions changed in the lock and notice files.

## Remaining evidence

Native local execution covers Windows/OpenUSD 26.08. The VRM implementation,
its baseline session update and native Linux/macOS downstream lanes remain to
be exercised. The checks certify adapter registration and package closure,
not rendered output or scene-index behavior. Report 48's RUNPATH request is
tracked separately.
